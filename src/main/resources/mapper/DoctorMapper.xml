<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- ClientMapper.java namespace -->
<mapper namespace="main.java.ru.timur.clinic.mapper.DoctorMapper">
    <!-- Person.java namespace -->
    <resultMap type="main.java.ru.timur.clinic.bean.Doctor" id="resultMap">
        <id property="id" column="ID" />
        <result property="firstname" column="FIRSTNAME"/>
        <result property="lastname" column="SURENAME"/>
        <result property="patronymic" column="PATRONIM"/>
        <result property="speciality" column="SPECIALITY"/>
        <result property="cabinet" column="CABINET"/>
        <result property="password" column="PASSWORD"/>
    </resultMap>

    <select id="getAllDoctors" resultMap="resultMap">
        SELECT * FROM doctors;
    </select>

    <select id="getDoctorById" resultMap="resultMap">
        SELECT * FROM  doctors where ID = #{id};
    </select>

    <select id="getDoctorByName" resultMap="resultMap">
        SELECT * FROM  doctors where FIRSTNAME = #{firstname};
    </select>

    <insert id="insertClient" parameterType="main.java.ru.timur.clinic.bean.Doctor">
        INSERT INTO  doctors (FIRSTNAME, SURENAME, PATRONIM, PHONE, ENABLED)
        VALUES (#{firstname}, #{lastname}, #{patronymic}, #{polis}, #{active});
    </insert>

    <insert id="updateClient" parameterType="main.java.ru.timur.clinic.bean.Doctor">
        UPDATE  doctors SET
        FIRSTNAME = #{firstname},
        LASTNAME = #{lastname},
        PATRONYMIC = #{patronymic},
        SPECIALITY = #{speciality},
        CABINET = #{cabinet},
        ACTIVE = #{active},
        PASSWORD = #{password};
    </insert>

</mapper>